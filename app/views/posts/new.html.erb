<h1>New Post</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for posts</p>
    <ul>
      <li>Make sure it rhymes.</li>
      <li>Don't use the letter "A".</li>
      <li>The incessant use of hashtags will get you banned</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for [@topic, @post] do |f| %>
      <div class="form-group">
        <!-- We use f.label to display title this functionality is provided by form_for
      which yiels a form building object that in turn provides f.label which in turn
    creates an HTML <label> tag for the object </label> -->
        <%= f.label :title %>
        <!-- f.text_field is another method from FormHelper that creates an <input> tag-->
          <%= f.text_field :title, class: 'form-control', placeholder: "Enter post title" %>
      </div>
      <div class="form-group">
          <%= f.label :body %>
          <%= f.text_area :body, class: 'form-control', placeholder: "Enter post body" %>
      </div>
      <div class="form-group">
        <!-- f.submit is another FormHelper method -->
          <%= f.submit "Save", class: 'btn btn-success' %>
      </div>
    <% end %>
  </div>
</div>
