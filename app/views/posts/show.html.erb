<h1><%= @post.title %></h1>

<div class="row">
  <div class="col-md-8">
    <p><%= @post.body %></p>
  </div>
  <div class="col-md-4">
  <!-- We format a link as an Edit button which directs a user to /posts/@post.id/edit
  edit_post_path(@post) is a helper method that in generated by routes.rb by resources :posts-->
    <%= link_to "Edit", edit_post_path(@post), class: 'btn btn-success' %>
  <!-- We use link_to to create a delete button. The text on button is Delete Post
    We override the default method (:post) with :delete so that when the button is pressed, the route called is the delete route.
    The hash confirms the action with a JS confirmation dialog when a user presses the button -->
    <%= link_to "Delete Post", @post, method: :delete, class: 'btn btn-danger', data: {confirm: 'Are you sure you want to delete this post?'} %>
  </div>
</div>
